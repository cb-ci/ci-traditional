removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: pipeline
  name: hw
  concurrentBuild: true
  definition:
    cpsFlowDefinition:
      sandbox: true
      script: |
        pipeline {
          agent none

          stages {
              stage('Hello') {
                  steps {
                      echo 'Hello World'
                  }
              }
          }
        }
  description: ''
  disabled: false
  displayName: hw
  properties:
    - gitLabConnection:
        useAlternativeCredential: false
        jobCredentialId: ''
        gitLabConnection: gitlab
  resumeBlocked: false